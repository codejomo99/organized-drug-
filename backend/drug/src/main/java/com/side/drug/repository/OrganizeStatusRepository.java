package com.side.drug.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.side.drug.model.OrganizeStatus;

public interface OrganizeStatusRepository extends JpaRepository<OrganizeStatus, Long> {
}

